# Python Projects

This directory contains Python-based projects and experiments.

## Projects

(Projects will be added here as they are uploaded)

## Running Python Projects

Most projects will require Python 3.x. Check individual project READMEs for specific requirements.

```bash
cd project-name
pip install -r requirements.txt  # if requirements exist
python main.py
```
